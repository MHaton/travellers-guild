import cn from 'classnames'

export const Image = ({ src, alt, className, legend, bleed, center, right, width, bordered }) => {
  return (
    <div className="not-prose block my-8">
      <img
        className={cn('w-full rounded-md', className, {
          'border border-neutral-200 dark:border-white/10': bordered,
          'mx-auto': center,
          'float-right': right,
          'w-1/4': width === "1/4",
          'w-1/3': width === "1/3",
          'w-1/2': width === "1/2",
          'w-3/4': width === "3/4",
          'w-2/3': width === "2/3",
        })}
        src={src}
        alt={alt}
      />
      {legend && (
        <p className="mt-4 text-sm text-center text-neutral-500 dark:text-white/50">{legend}</p>
      )}
    </div>
  )
}
